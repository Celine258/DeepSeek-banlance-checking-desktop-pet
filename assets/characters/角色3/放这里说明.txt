把角色的两帧图片放进这个文件夹，桌宠就会自动认出来。

需要两个文件（PNG，透明底）：

    idle.png    平时状态 —— 闭眼 / 平静
    active.png  点击状态 —— 睁开眼 + 一脸邪气又淘气的笑

两张图的画布尺寸必须完全一样，人物的位置、大小也必须完全对齐，
否则点击的时候整个角色会「跳」一下，而不是只有表情变。

想改右键菜单里显示的名字：新建一个 name.txt，第一行写名字就行。
想改文件夹名也可以，菜单里默认就用文件夹名。

详细规格见项目根目录的《素材规格.md》。
