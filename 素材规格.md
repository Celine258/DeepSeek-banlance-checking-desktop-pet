# 素材规格 —— 你要准备的图片

程序已经写好了，就差图。按下面这个规格做，放进文件夹就生效，不用改任何代码。

---

## 一、总览

| 项目 | 要求 |
| --- | --- |
| 文件格式 | PNG，**必须带透明通道**（不要白底、不要灰底） |
| 每个角色几张 | 2 张：`idle.png`（平时）＋ `active.png`（点击时） |
| 一共几个角色 | 4 个 → 一共 8 张图 |
| 建议画布尺寸 | 800 × 1200 像素（像素多一点没关系，程序会自动缩放） |
| 画布尺寸 | **同一个角色的两张图必须一模一样大**，最好 8 张全部统一 |
| 人物位置 | **8 张图里人物的大小、位置、脚底基线必须完全对齐** |

---

## 二、文件夹怎么摆

放成这个样子（文件夹已经建好了，直接往里面丢图）：

```
deepseek-pet/
└── assets/
    └── characters/
        ├── 角色1/
        │   ├── idle.png      ← 平时：闭着眼、表情平静
        │   ├── active.png    ← 点击：睁开眼 + 一脸邪气又淘气的笑
        │   └── name.txt      ← 可选，第一行写显示在菜单里的名字
        ├── 角色2/
        │   ├── idle.png
        │   └── active.png
        ├── 角色3/
        └── 角色4/
```

说明：

- 文件夹名随便起，默认就把文件夹名当作右键菜单里显示的名字。想让菜单显示「银狼」这种名字，要么把文件夹改成那个名字，要么在文件夹里放一个 `name.txt`，第一行写名字。
- 文件名除了 `idle.png` / `active.png`，程序也认这几个：`平时.png`、`普通.png`、`点击.png`、`睁眼.png`、`邪笑.png`，以及对应的 `.webp`。
- 加角色不需要改代码：丢一个新文件夹进去，右键菜单里就会自动多一项（有 2 秒缓存，最多等两秒）。

---

## 三、两张图怎么对齐（最关键的一条）

你要求「点击之后只有表情变，其他所有地方不动」。这一条能不能成立，**完全取决于这两张图对齐得准不准**。

程序的做法很简单：把 `idle.png` 和 `active.png` 叠在同一个位置上，点击时只把上面那层的透明度从 0 调到 1。所以只要两张图有 1 像素的错位，整个人物就会抖一下。

正确做法：

1. 先画好 `idle.png`。
2. 复制一份改名为 `active.png`，**在同一个画布上**只改脸（眼睛、眉毛、嘴），身体、头发、衣服、四肢一像素都不要碰。
3. 两张图都不要去裁切、不要去「自动裁掉透明边」——裁剪是错位最常见的来源。

一个自检方法：把两张图叠在一起做「差值」对比，如果除了脸部区域之外全是黑的，就是完美的。

---

## 四、透明底怎么做

如果你手上只有带背景的图（比如我一开始收到的那 5 张），需要先把背景抠掉。几种办法：

- **Photoshop / 稿定 / 美图秀秀**：选择主体 → 抠出人物 → 删掉背景图层 → 导出 PNG（务必勾选「透明度」）。
- **在线工具**：remove.bg、佐糖这类一键抠图，对人物立绘效果通常够用。
- **AI 出图工具**：如果这两帧本来就是生成的，大多数工具可以直接要求「透明背景 PNG 输出」或「去背景」。
- **PowerPoint**：图片 → 删除背景，也能用，但边缘会糙一点。

抠完之后检查一下：把图放在深色背景和浅色背景上看一遍，如果人物边缘有一圈白边/灰边，说明是「白底抠图」留下的毛边，影响观感但不算致命，能修就修。

---

## 五、画面风格建议（不是硬性要求）

- 人物的**脚底**尽量贴着画布下边缘往上留一点点空隙（比如 2% 的画布高度），这样站在任务栏上方会更自然。
- 人物**左右居中**。
- 点击状态的「邪气又淘气的笑」建议：眼睛睁开、瞳孔偏亮、嘴角往一边挑、眉毛压低。跟平时那张的差别越集中在脸部，效果越好。
- 不需要给人物加阴影或发光——那些会被当成人物的一部分一起显示出来。

---

## 六、放好之后怎么验证

1. 双击 `启动.bat`。
2. 角色应该直接出现了。如果显示的还是那个灰色剪影的占位图，说明图没被认出来，检查：
   - 文件是不是真的叫 `idle.png`（Windows 默认隐藏扩展名，很容易变成 `idle.png.png`）；
   - 是不是放在 `assets/characters/角色1/` 这一层，而不是再套了一层文件夹；
   - 图片是不是真的带透明通道。
3. 右键角色 → 菜单里应该能看到你的角色名字。
4. 左键点一下角色：应该只有表情变，同时左上方弹出余额气泡。
